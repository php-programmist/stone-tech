<div class="products">
    {% for product in products %}
        <div class="product">
            <a href="/{{ product.path }}" target="_blank">
                <img class="b-lazy" data-src="/uploads/products/{{ product.image }}" alt="{{ product.name }}">
            </a>
            <div class="product__name">
                <a href="/{{ product.path }}" target="_blank">
                    {{ product.name }}
                </a>
            </div>
            {#{% if category.id != 21 and category.id != 22 and category.id != 23 %}#}
            {% if hidePriceArray is defined and  hidePriceArray == false %}
                <div class="product__price">от {{ product.price}} {{ product.measure.name }}</div>
            {% endif %}
            <button class="popup-button raschet">Рассчитать</button>
            <button class="popup-button raschet">Консультация</button>
        </div>
    {% endfor %}
</div>
<div class="navigation">
    {{ knp_pagination_render(pagination) }}
</div>
